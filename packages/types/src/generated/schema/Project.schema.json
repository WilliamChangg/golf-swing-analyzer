{
  "$defs": {
    "AnchorResidual": {
      "description": "How far one anchor sits from the fitted map.\n\nSigned, and in milliseconds, because the sign is the informative part: four\nresiduals that alternate are noise, and four that trend are a rate the fit\nwas not allowed to take up.",
      "properties": {
        "label": {
          "type": "string"
        },
        "reference_s": {
          "type": "number"
        },
        "observed_target_s": {
          "type": "number"
        },
        "predicted_target_s": {
          "type": "number"
        },
        "residual_ms": {
          "description": "observed - predicted, in milliseconds.",
          "type": "number"
        }
      },
      "required": [
        "label",
        "reference_s",
        "observed_target_s",
        "predicted_target_s",
        "residual_ms"
      ],
      "title": "AnchorResidual",
      "type": "object"
    },
    "AnchorSource": {
      "description": "Where one anchor's pair of frames came from.\n\nKept distinct from `SyncMethod` because a single fit may mix them: a person\ncorrecting one badly-detected event leaves the other three as detections,\nand the result is neither wholly manual nor wholly automatic.",
      "enum": [
        "detected",
        "manual"
      ],
      "title": "AnchorSource",
      "type": "string"
    },
    "BoardFamily": {
      "description": "Which ArUco dictionary the board's markers are drawn from.\n\nCarried rather than fixed because the board is a physical object that the\nuser printed, and a detector looking for the wrong dictionary finds nothing\nat all -- a failure that looks exactly like bad lighting.\n\nThe default is `DICT_5X5_100`: 5x5 markers stay legible at the size a\nCharuco square allows on an A4 sheet, and 100 of them is far more than a\nboard this size consumes, so ids never wrap.",
      "enum": [
        "DICT_4X4_50",
        "DICT_4X4_100",
        "DICT_5X5_100",
        "DICT_5X5_250",
        "DICT_6X6_250"
      ],
      "title": "BoardFamily",
      "type": "string"
    },
    "BoardObservation": {
      "description": "One frame in which the board was found.\n\nKept per frame rather than summarised because 8.4 draws them: the review UI\nshows where in the image each view landed, which is how a person sees that\ntheir board never reached a corner -- a fact no single number communicates\nas quickly as the picture of it does.",
      "properties": {
        "frame": {
          "type": "integer"
        },
        "corners": {
          "description": "Chessboard corners identified in this view.",
          "type": "integer"
        },
        "reprojection_rms_px": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "This view's own RMS residual after the fit. None before a fit, and for a view the fit excluded."
        },
        "tilt_deg": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "How far off square to the camera this view sat."
        },
        "distance_m": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Board distance along the optical axis. Metric, because the board's own square size supplies the scale -- the first genuinely metric length in this engine."
        },
        "centroid_x": {
          "description": "Mean corner position, in pixels.",
          "type": "number"
        },
        "centroid_y": {
          "type": "number"
        },
        "used": {
          "default": true,
          "description": "Whether this view entered the fit, or was dropped.",
          "type": "boolean"
        },
        "dropped_reason": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        }
      },
      "required": [
        "frame",
        "corners",
        "centroid_x",
        "centroid_y"
      ],
      "title": "BoardObservation",
      "type": "object"
    },
    "BoardSpec": {
      "description": "The physical board, as printed.\n\nCharuco rather than a plain chessboard, for two reasons that both matter on\nfootage taken by one person with no assistant. A chessboard must be seen\n**whole** or it yields nothing, so every view where a corner leaves the\nframe is wasted -- which is exactly the view that carries the distortion\ninformation. And its corners are interchangeable, so a rotationally\nsymmetric view is ambiguous. Charuco's markers identify each corner\nindividually, so a partial view contributes the corners it does show and the\ncorrespondence is never in doubt.\n\n`square_length_m` is what puts a metre into the system. Every metric-scale\nclaim Phase 9 makes descends from this one measured number, so it is the\nlength to measure on the printed sheet with a ruler rather than to take from\nthe file that was sent to the printer: printers scale to fit, and a page\nscaled to 96% makes every future distance wrong by 4% with nothing\nanywhere looking amiss.",
      "properties": {
        "squares_x": {
          "description": "Chessboard squares across.",
          "minimum": 2,
          "type": "integer"
        },
        "squares_y": {
          "description": "Chessboard squares down.",
          "minimum": 2,
          "type": "integer"
        },
        "square_length_m": {
          "description": "Side of one chessboard square, in metres, **measured on the printed sheet**. The scale of every metric claim descends from this number.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        "marker_length_m": {
          "description": "Side of one ArUco marker, in metres. Must be smaller than the square it sits in; around 0.75 of it leaves a white margin that the detector needs to find the marker's border.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        "family": {
          "allOf": [
            {
              "$ref": "#/$defs/BoardFamily"
            }
          ],
          "default": "DICT_5X5_100"
        },
        "legacy_pattern": {
          "default": false,
          "description": "Whether the board was generated by OpenCV before 4.6, which laid the markers out differently. A board printed from an old generator and detected as a new one produces corners in the wrong places rather than no corners at all, which is the worse failure. False for any board this build generated.",
          "type": "boolean"
        }
      },
      "required": [
        "squares_x",
        "squares_y",
        "square_length_m",
        "marker_length_m"
      ],
      "title": "BoardSpec",
      "type": "object"
    },
    "CalibrationConfig": {
      "additionalProperties": false,
      "description": "Policy for calibration.\n\nThe bounds are **stated policy informed by measurement**, not measured\noptima: `scripts/benchmark_calibration.py` establishes the shape of each\nrelationship -- that focal uncertainty explodes as tilt range collapses,\nfor instance -- and where on that curve to refuse is a judgement about what\na wrong answer costs downstream. Where a number came from a sweep, the field\nsays so.",
      "properties": {
        "distortion_model": {
          "allOf": [
            {
              "$ref": "#/$defs/DistortionModel"
            }
          ],
          "default": "radial_tangential_4",
          "description": "Which coefficients to fit. Four rather than OpenCV's five, on the measurement: with the board reaching the frame edge the fifth term buys almost nothing, and without it the fifth term is fitted to noise and does its damage at the corners where the swing is. `scripts/benchmark_calibration.py --sweep distortion` has the numbers, and a fisheye lens is the case for overriding this."
        },
        "min_views": {
          "default": 8,
          "description": "Board views required before a calibration is attempted. Three is the algebraic minimum for a planar target and is nowhere near enough in practice; the usual advice of 15-20 is about coverage rather than count, which `CoverageReport` measures directly. This is a floor below which the coverage numbers themselves stop meaning much.",
          "minimum": 3,
          "type": "integer"
        },
        "min_corners_per_view": {
          "default": 6,
          "description": "Corners a view must contribute to be used. Four is the minimum for a pose; six leaves something over, and a view showing fewer is usually the board leaving the frame rather than a useful oblique.",
          "minimum": 4,
          "type": "integer"
        },
        "min_image_fraction": {
          "default": 0.35,
          "description": "Fraction of the frame the board must have visited. Below it the lens is unmeasured over most of its area, and the distortion coefficients are extrapolating into the region the subject is filmed in.",
          "maximum": 1.0,
          "minimum": 0.0,
          "type": "number"
        },
        "min_tilt_range_deg": {
          "default": 20.0,
          "description": "Spread of board tilts required. **The bound that catches the failure neither the residual nor the reported uncertainty can see.** With every view square to the sensor, focal length and distance are very nearly interchangeable, and the fit is free to pick badly while fitting beautifully.\n\nMeasured (`--sweep tilt`): at 0-2 degrees of spread the focal length comes out 6-10% wrong at an RMS of 0.21 px; at 20 degrees it is 0.10% wrong at an RMS of 0.25 px. Twenty is where the error collapses, and it is far more tilt than anyone holding a board produces by accident, which is the point.",
          "minimum": 0.0,
          "type": "number"
        },
        "max_focal_uncertainty_ratio": {
          "default": 0.02,
          "description": "Standard deviation of the fitted focal length, as a fraction of it, above which the calibration is refused. Two percent of focal length is roughly two percent of every triangulated depth, which is a centimetre at half a metre and compounds through Phase 9.\n\nA backstop rather than the main instrument: it catches a fit that is visibly ill-conditioned and, as `fx_uncertainty` documents, misses the degenerate capture entirely. The coverage bounds are what catch that, and they are checked first.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        "max_rms_reprojection_px": {
          "default": 1.0,
          "description": "Residual above which the fit is refused outright. A pixel is a loose bound -- a good calibration lands well under half of one -- and it is deliberately loose, because this bound catches gross failure (a mis-specified board, a wrong dictionary) rather than mediocrity, which the uncertainty bound above is the instrument for.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        "max_focal_disagreement": {
          "default": 0.05,
          "description": "How far fx and fy may differ, as a fraction of their mean. Consumer sensors have square pixels, so a real disagreement is evidence of a bad fit rather than of an unusual camera.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        "max_pairing_error_px": {
          "default": 1.0,
          "description": "Board displacement attributable to imperfect pairing, above which a stereo pair is dropped. One pixel, so that the sync error a pair contributes stays below the reprojection error it would otherwise be mistaken for. Holding the board still is what keeps pairs inside this; nothing else has to.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        "max_pair_time_error_ms": {
          "default": 50.0,
          "description": "How far apart, on one clock, two frames of a pair may sit. A backstop for the bound above rather than the main instrument: a perfectly still board would satisfy the pixel test at any time error, and at some separation the assumption that nothing else in the scene moved stops being reasonable.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        "min_stereo_pairs": {
          "default": 6,
          "description": "Paired board views required for stereo extrinsics. Fewer resolves the pose but leaves nothing over to check it with, and a stereo calibration with no spare degrees of freedom has a residual of zero by construction.",
          "minimum": 3,
          "type": "integer"
        }
      },
      "title": "CalibrationConfig",
      "type": "object"
    },
    "CalibrationQuality": {
      "description": "How good a calibration is, in the three senses that differ.\n\nSee the module docstring. The short version: `rms_reprojection_px` is the\nfit, the parameter uncertainties on `CameraIntrinsics` are the answer, and\n`coverage` is the cause. A tool that reports only the first is reporting the\none of the three that a degenerate capture makes *better*.",
      "properties": {
        "rms_reprojection_px": {
          "description": "Root-mean-square distance between each detected corner and where the fitted model puts it. How well the model fits the data it was given -- necessary, and on its own not evidence that the data determined the model.",
          "minimum": 0.0,
          "type": "number"
        },
        "max_reprojection_px": {
          "description": "Worst single corner residual. Separates a uniformly mediocre fit from a good one with a misdetected view in it, which call for different responses.",
          "minimum": 0.0,
          "type": "number"
        },
        "per_view_rms_px": {
          "description": "Residual per board view, in the order the views were used. Drawn by 8.4.",
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "coverage": {
          "$ref": "#/$defs/CoverageReport"
        },
        "degrees_of_freedom": {
          "description": "Corner observations times two, minus the parameters fitted. The same guard `SyncQuality.degrees_of_freedom` provides one layer down: a residual from a fit with nothing spare is not evidence.",
          "type": "integer"
        }
      },
      "required": [
        "rms_reprojection_px",
        "max_reprojection_px",
        "coverage",
        "degrees_of_freedom"
      ],
      "title": "CalibrationQuality",
      "type": "object"
    },
    "CameraCalibration": {
      "description": "One camera's calibration: what was measured, from what, and how well.\n\n`usable` and `refusal` are the gate. A calibration that exists is not a\ncalibration that may be used, and the difference is decided here rather than\nby each consumer, so a consumer that forgets to check the coverage cannot\nsilently use a set of views that never determined a focal length.",
      "properties": {
        "schema_version": {
          "default": 1,
          "type": "integer"
        },
        "role": {
          "$ref": "#/$defs/CameraRole"
        },
        "intrinsics": {
          "$ref": "#/$defs/CameraIntrinsics"
        },
        "quality": {
          "$ref": "#/$defs/CalibrationQuality"
        },
        "detection": {
          "$ref": "#/$defs/DetectionReport"
        },
        "calibrated_at": {
          "format": "date-time",
          "type": "string"
        },
        "source": {
          "description": "What the board views came from, for provenance.",
          "type": "string"
        },
        "notes": {
          "default": "",
          "description": "Free text for the capture setting a video file cannot record: which lens, what zoom, whether stabilisation was off. The failure this field exists for is a correct calibration applied to footage shot at a different zoom, which nothing can detect.",
          "type": "string"
        },
        "usable": {
          "description": "Whether this calibration passed the bounds in `CalibrationConfig`.",
          "type": "boolean"
        },
        "refusal": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Why it did not, in words. Set exactly when `usable` is false."
        },
        "warnings": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "role",
        "intrinsics",
        "quality",
        "detection",
        "calibrated_at",
        "source",
        "usable"
      ],
      "title": "CameraCalibration",
      "type": "object"
    },
    "CameraIntrinsics": {
      "description": "One camera's interior geometry, measured.\n\nStated as four scalars rather than a 3x3 matrix because that is what was\nfitted: the skew term of a general K is zero for any sensor manufactured in\nthe last forty years, and carrying a full matrix would invite a reader to\nbelieve this build had measured it. `matrix()` assembles the conventional\nform for the arithmetic that wants one.\n\nPixel coordinates here are **displayed** pixels, after Phase 1's rotation has\nbeen applied -- the same frame the landmarks are normalised against. A\ncalibration measured on the stored orientation and applied to the displayed\none has fx and fy interchanged, which is a plausible-looking calibration that\nis wrong everywhere.",
      "properties": {
        "fx": {
          "description": "Focal length in pixels, horizontal.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        "fy": {
          "description": "Focal length in pixels, vertical.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        "cx": {
          "description": "Optical centre, horizontal, in pixels from the left.",
          "type": "number"
        },
        "cy": {
          "description": "Optical centre, vertical, in pixels from the top.",
          "type": "number"
        },
        "distortion": {
          "description": "Distortion coefficients in OpenCV's order (k1, k2, p1, p2[, k3]), as many as `model` declares.",
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "model": {
          "$ref": "#/$defs/DistortionModel"
        },
        "image_width": {
          "description": "Displayed frame width this was measured at.",
          "exclusiveMinimum": 0,
          "type": "integer"
        },
        "image_height": {
          "description": "Displayed frame height this was measured at.",
          "exclusiveMinimum": 0,
          "type": "integer"
        },
        "fx_uncertainty": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Standard deviation of `fx`, in pixels, propagated from the fit's residuals through its Jacobian.\n\n**It does not catch a degenerate capture, which is what the design here expected of it.** Measured, it is smallest exactly where the answer is worst -- 0.012% on a capture whose focal length is wrong by 6%, against 0.31% on one that is right to 0.05% -- because the distortion coefficients absorb the degeneracy and leave a tightly determined wrong answer. A covariance computed from one set of views cannot see outside them. Reported because it is a real statement about the fit's conditioning, and not what `usable` rests on; `CoverageReport` is. None where the fit did not report one."
        },
        "fy_uncertainty": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "cx_uncertainty": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "cy_uncertainty": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        }
      },
      "required": [
        "fx",
        "fy",
        "cx",
        "cy",
        "distortion",
        "model",
        "image_width",
        "image_height"
      ],
      "title": "CameraIntrinsics",
      "type": "object"
    },
    "CameraRig": {
      "description": "Everything known about the geometry of one project's cameras.\n\nStored whole, one per project, for the reason `ProjectSync` is stored whole:\nit is a nested document read entire and never queried by part, and the\nPydantic model is already the authoritative definition of its shape.\n\nKeeping both cameras and their relation in one document rather than three\nrows is what makes `status` computable without a join, and `status` is the\nthing every consumer actually asks for.",
      "properties": {
        "schema_version": {
          "default": 1,
          "type": "integer"
        },
        "cameras": {
          "additionalProperties": {
            "$ref": "#/$defs/CameraCalibration"
          },
          "propertyNames": {
            "$ref": "#/$defs/CameraRole"
          },
          "type": "object"
        },
        "stereo": {
          "anyOf": [
            {
              "$ref": "#/$defs/StereoCalibration"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "config": {
          "$ref": "#/$defs/CalibrationConfig"
        }
      },
      "title": "CameraRig",
      "type": "object"
    },
    "CameraRole": {
      "description": "Where a camera was put, as the user declared it.\n\nDeliberately the *declared* role, not the measured one. Phase 6's\n`CameraView` is measured from the shoulder line at address and is the\nauthority on what the footage contains; this is the authority on what the\nuser intended, and the interesting case is the two disagreeing.\n\nOTHER exists so a third camera, or a phone propped at an angle nobody would\ncall either name, can still be part of a project instead of forcing a\ndishonest label.",
      "enum": [
        "face_on",
        "down_the_line",
        "other"
      ],
      "title": "CameraRole",
      "type": "string"
    },
    "ClipRef": {
      "description": "One clip of a synchronised pair, as the sync layer needs to see it.\n\nTimings here are **real seconds**, already divided by the slow-motion factor,\nmatching every other duration in the engine. `slow_motion_factor` is carried\nalongside so a reader can recover the clip's own playback clock, and because\na fitted rate far from 1.0 usually means one of these two numbers is wrong.",
      "properties": {
        "path": {
          "type": "string"
        },
        "name": {
          "description": "Basename, for display.",
          "type": "string"
        },
        "frames": {
          "type": "integer"
        },
        "start_s": {
          "description": "Timestamp of the first frame, in real seconds.",
          "type": "number"
        },
        "duration_s": {
          "description": "Span from the first to the last frame, in real seconds.",
          "type": "number"
        },
        "median_interval_s": {
          "description": "Median gap between frames, in real seconds. The quantisation of every instant located in this clip, and half of what bounds how well the pair can be aligned.",
          "type": "number"
        },
        "slow_motion_factor": {
          "description": "The factor the caller supplied for this clip. 1.0 is real time.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        }
      },
      "required": [
        "path",
        "name",
        "frames",
        "start_s",
        "duration_s",
        "median_interval_s",
        "slow_motion_factor"
      ],
      "title": "ClipRef",
      "type": "object"
    },
    "ContentKey": {
      "description": "Identity of a file's contents, used to key derived artifacts.\n\nThe algorithm travels with the digest so a cache written under one scheme is\nnever silently compared against a digest produced by another.",
      "properties": {
        "algorithm": {
          "$ref": "#/$defs/HashAlgorithm"
        },
        "digest": {
          "description": "Lowercase hex sha256 digest.",
          "type": "string"
        },
        "size_bytes": {
          "description": "File size at the time the digest was taken.",
          "type": "integer"
        }
      },
      "required": [
        "algorithm",
        "digest",
        "size_bytes"
      ],
      "title": "ContentKey",
      "type": "object"
    },
    "CorrelationReport": {
      "description": "The independent estimate, and what it had to work with.\n\nCross-correlating two *projected* speed signals assumes their temporal shapes\nmatch, which they do not exactly: a face-on camera sees the full sweep of the\nhands and a down-the-line camera sees much of it foreshortened, so the same\nswing produces two differently-shaped curves. The instants survive the\nprojection -- the hands are slowest at the top and fastest near impact from\nevery angle -- which is why this works at all, and `peak_correlation` is how\nwell it worked on this pair rather than an assumption that it did.",
      "properties": {
        "peak_correlation": {
          "description": "Pearson correlation at the best lag.",
          "maximum": 1.0,
          "minimum": -1.0,
          "type": "number"
        },
        "peak_offset_s": {
          "description": "The lag that maximised it, as a target-minus-reference offset.",
          "type": "number"
        },
        "rival_correlation": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The best correlation at any lag far enough from the peak to be a different alignment. None when no such lag scored at all."
        },
        "rival_offset_s": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Where that rival sat. A swing has two speed humps, so a rival is expected."
        },
        "grid_interval_s": {
          "description": "Resampling interval both signals were put on: the finer of the two clips'.",
          "type": "number"
        },
        "overlap_s": {
          "description": "Seconds of signal both clips supplied at the best lag.",
          "type": "number"
        },
        "samples": {
          "description": "Grid samples that contributed to the peak correlation.",
          "type": "integer"
        },
        "sub_grid_shift_s": {
          "description": "How far the parabolic refinement moved the peak off the grid. Bounded by half a grid step by construction; a value at that bound means the refinement saturated and the peak is not well shaped.",
          "type": "number"
        }
      },
      "required": [
        "peak_correlation",
        "peak_offset_s",
        "grid_interval_s",
        "overlap_s",
        "samples",
        "sub_grid_shift_s"
      ],
      "title": "CorrelationReport",
      "type": "object"
    },
    "CoverageReport": {
      "description": "What the board views actually sampled, and therefore what they could fix.\n\nThe diagnostic half of a calibration. Each field detects a different\ndegeneracy, and every one of them is invisible in the reprojection residual\n-- a degenerate set fits *better*, because the model has less to disagree\nwith. See the module docstring for why each matters.",
      "properties": {
        "views": {
          "description": "Board views that entered the fit.",
          "type": "integer"
        },
        "corners": {
          "description": "Chessboard corners summed over those views.",
          "type": "integer"
        },
        "image_fraction": {
          "description": "Fraction of the frame, on a coarse grid, containing at least one detected corner. The blunt measure of whether the lens was sampled where it bends.",
          "maximum": 1.0,
          "minimum": 0.0,
          "type": "number"
        },
        "edge_fraction": {
          "description": "Fraction of all detected corners lying in the outer fifth of the frame radius. Distortion is a function of radius and is nearly nothing at the centre, so this is the share of the evidence that carries any information about it at all.",
          "maximum": 1.0,
          "minimum": 0.0,
          "type": "number"
        },
        "tilt_range_deg": {
          "description": "Spread between the least and most tilted board view, in degrees off square to the camera. **The field that catches the worst case**: a board always held parallel to the sensor cannot separate focal length from distance, and produces an excellent residual around a focal length that is simply not determined.",
          "minimum": 0.0,
          "type": "number"
        },
        "scale_range": {
          "description": "Largest apparent board size over smallest, across views. 1.0 means every view was taken at one distance.",
          "minimum": 1.0,
          "type": "number"
        },
        "methodology": {
          "type": "string"
        }
      },
      "required": [
        "views",
        "corners",
        "image_fraction",
        "edge_fraction",
        "tilt_range_deg",
        "scale_range",
        "methodology"
      ],
      "title": "CoverageReport",
      "type": "object"
    },
    "DetectionReport": {
      "description": "What board detection found across a source, and what it discarded.\n\nEvery count is here rather than only the survivors, because the ratios are\nthe diagnostic. Frames scanned against frames with a board found says\nwhether the board was visible; found against used says whether the views\nwere good enough; and a large gap in either has a different fix -- reshoot\nwith the board in frame, or reshoot with it closer and steadier.",
      "properties": {
        "frames_scanned": {
          "type": "integer"
        },
        "frames_with_board": {
          "type": "integer"
        },
        "views_used": {
          "type": "integer"
        },
        "corners_total": {
          "type": "integer"
        },
        "board": {
          "$ref": "#/$defs/BoardSpec"
        },
        "observations": {
          "items": {
            "$ref": "#/$defs/BoardObservation"
          },
          "type": "array"
        },
        "warnings": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "frames_scanned",
        "frames_with_board",
        "views_used",
        "corners_total",
        "board"
      ],
      "title": "DetectionReport",
      "type": "object"
    },
    "DistortionModel": {
      "description": "Which lens-distortion coefficients were fitted.\n\nNamed and chosen rather than defaulted, because a coefficient that is not\ndetermined by the data does not sit harmlessly near zero -- it takes a value\nthat cancels some of the residual in the region the board happened to cover\nand then diverges outside it, which is the region a swing is filmed in.\n\nPINHOLE\n    No distortion. Correct only for a lens that has none, which no phone\n    wide-angle has; useful as a null model and as the thing a test can\n    recover exactly.\nRADIAL_TANGENTIAL_4\n    k1, k2, p1, p2. Two radial terms and the tangential pair that models a\n    lens not quite parallel to the sensor. The default here, on the\n    measurement in `scripts/benchmark_calibration.py`.\nRADIAL_TANGENTIAL_5\n    The above plus k3. OpenCV's own default, and a third radial term is\n    genuinely needed by a fisheye or an action camera -- but it is the term\n    that most readily absorbs noise when the board never reaches the corners,\n    and it does its damage exactly there.",
      "enum": [
        "pinhole",
        "radial_tangential_4",
        "radial_tangential_5"
      ],
      "title": "DistortionModel",
      "type": "string"
    },
    "HashAlgorithm": {
      "description": "How a `ContentKey` digest was computed.\n\nSHA256          - the whole file. Use when the answer must be an integrity\n                  check, as it is for model weights.\nSHA256_SAMPLED  - size, plus sha256 over three fixed windows of the file.\n                  **This is not an integrity check.** It exists because\n                  hashing every byte of a multi-gigabyte recording on every\n                  open costs seconds, and a cache key only has to change\n                  when the file changes, not prove that it did not.",
      "enum": [
        "sha256",
        "sha256-sampled-v1"
      ],
      "title": "HashAlgorithm",
      "type": "string"
    },
    "OverlapReport": {
      "description": "What the two clips have in common once the map is applied.\n\nStated in reference-clip time, because that is the frame the map is\nparameterised in and a reader comparing it against `ClipRef.duration_s` needs\nthe two in the same clock.",
      "properties": {
        "start_s": {
          "type": "number"
        },
        "end_s": {
          "type": "number"
        },
        "duration_s": {
          "type": "number"
        },
        "reference_fraction": {
          "description": "Fraction of the reference clip inside the overlap.",
          "maximum": 1.0,
          "minimum": 0.0,
          "type": "number"
        },
        "target_fraction": {
          "description": "Fraction of the target clip inside the overlap.",
          "maximum": 1.0,
          "minimum": 0.0,
          "type": "number"
        }
      },
      "required": [
        "start_s",
        "end_s",
        "duration_s",
        "reference_fraction",
        "target_fraction"
      ],
      "title": "OverlapReport",
      "type": "object"
    },
    "PairingReport": {
      "description": "How the two cameras' board views were paired, and what that cost.\n\nThe measurement that lets an unsynchronised pair be stereo-calibrated at\nall. `worst_pairing_error_px` is the time map's uncertainty converted into\nthe unit it contaminates, by multiplying it by the board's observed image\nspeed: a still board makes it nearly zero however badly the clocks are\nknown, and a moving one makes it large however well they are. See the module\ndocstring.",
      "properties": {
        "pairs": {
          "description": "Frame pairs that entered the stereo fit.",
          "type": "integer"
        },
        "candidates": {
          "description": "Pairs considered before the bounds below dropped any.",
          "type": "integer"
        },
        "method": {
          "description": "How frames were matched: 'time_map' or 'explicit'.",
          "type": "string"
        },
        "median_time_error_ms": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Median distance between the two frames of a pair once mapped onto one clock. None for explicit pairs, where the caller asserted simultaneity and nothing here can check it."
        },
        "worst_pairing_error_px": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Largest board displacement, in pixels, attributable to the pairing being imperfect: the sync uncertainty there times the board's image speed there. Directly comparable with the reprojection error, because it is the same unit and it adds to it."
        },
        "median_board_speed_px_s": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "How fast the board was moving in the image across the used pairs. The number the capture instruction acts on: hold it still and this goes to zero, and with it the whole cost of not having a genlock."
        },
        "dropped": {
          "description": "Why each rejected candidate was rejected.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "pairs",
        "candidates",
        "method"
      ],
      "title": "PairingReport",
      "type": "object"
    },
    "ProjectClip": {
      "description": "One video belonging to a project.",
      "properties": {
        "id": {
          "type": "integer"
        },
        "role": {
          "$ref": "#/$defs/CameraRole"
        },
        "path": {
          "type": "string"
        },
        "name": {
          "description": "Basename, for display.",
          "type": "string"
        },
        "content_key": {
          "$ref": "#/$defs/ContentKey"
        },
        "slow_motion_factor": {
          "description": "How many times slower than real time this clip plays, as supplied when it was added. Stored per clip because two cameras in one session routinely differ -- one phone at 240 fps and one at 30.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        "added_at": {
          "format": "date-time",
          "type": "string"
        },
        "exists": {
          "description": "Whether a file is still at `path`. False does not invalidate the project: the content key still identifies the clip and its cached extraction, and the fix is to point the project at the moved file.",
          "type": "boolean"
        },
        "label": {
          "default": "",
          "description": "Free text the user attached. Never interpreted.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "role",
        "path",
        "name",
        "content_key",
        "slow_motion_factor",
        "added_at",
        "exists"
      ],
      "title": "ProjectClip",
      "type": "object"
    },
    "ProjectSync": {
      "description": "A stored alignment between two of a project's clips.\n\nThe clip ids are here as well as inside the model because the model\nidentifies its clips by path, and a project's clips can be re-pointed at\nmoved files. The ids are what survive that.",
      "properties": {
        "reference_clip_id": {
          "type": "integer"
        },
        "target_clip_id": {
          "type": "integer"
        },
        "model": {
          "$ref": "#/$defs/SyncModel"
        },
        "updated_at": {
          "format": "date-time",
          "type": "string"
        }
      },
      "required": [
        "reference_clip_id",
        "target_clip_id",
        "model",
        "updated_at"
      ],
      "title": "ProjectSync",
      "type": "object"
    },
    "StereoCalibration": {
      "description": "Where the second camera stands relative to the first, measured.\n\nStated as the rotation and translation taking a point from the reference\ncamera's frame into the target's. The baseline is reported separately\nbecause it is the one number in here a person can check with a tape measure,\nand a stereo calibration whose baseline is wrong by a factor of two is\nusually a board whose `square_length_m` was taken from the file rather than\nthe print.",
      "properties": {
        "schema_version": {
          "default": 1,
          "type": "integer"
        },
        "reference_role": {
          "$ref": "#/$defs/CameraRole"
        },
        "target_role": {
          "$ref": "#/$defs/CameraRole"
        },
        "rotation": {
          "description": "3x3 rotation, reference frame to target.",
          "items": {
            "items": {
              "type": "number"
            },
            "type": "array"
          },
          "type": "array"
        },
        "translation_m": {
          "description": "3-vector, in metres, in the target's frame.",
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "baseline_m": {
          "description": "Distance between the two optical centres. Checkable with a tape.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        "convergence_deg": {
          "description": "Angle between the two optical axes. Near zero is a parallel rig; a face-on and a down-the-line camera are near 90, which is the best possible geometry for triangulation and the worst for finding anything visible in both.",
          "type": "number"
        },
        "quality": {
          "$ref": "#/$defs/CalibrationQuality"
        },
        "pairing": {
          "$ref": "#/$defs/PairingReport"
        },
        "calibrated_at": {
          "format": "date-time",
          "type": "string"
        },
        "usable": {
          "type": "boolean"
        },
        "refusal": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "warnings": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "reference_role",
        "target_role",
        "rotation",
        "translation_m",
        "baseline_m",
        "convergence_deg",
        "quality",
        "pairing",
        "calibrated_at",
        "usable"
      ],
      "title": "StereoCalibration",
      "type": "object"
    },
    "SwingEvent": {
      "description": "The instants that divide a swing.\n\nTAKEAWAY - the hands begin moving away from the ball.\nTOP      - the backswing reverses; the hands are momentarily near rest.\nIMPACT   - estimated from hand kinematics, not observed. See module docstring.\nFINISH   - the hands come back to rest after the follow-through.",
      "enum": [
        "takeaway",
        "top",
        "impact",
        "finish"
      ],
      "title": "SwingEvent",
      "type": "string"
    },
    "SyncAnchor": {
      "description": "One instant identified in both clips.\n\nFrames and seconds are both carried. Frames are what a person picks and what\na UI scrubs to; seconds are what the map is fitted in, and on\nvariable-rate footage the two are not interconvertible without the clip's own\ntimestamps -- which live in the engine, not in the caller.",
      "properties": {
        "label": {
          "description": "What this instant is, in words. Shown to a reader.",
          "type": "string"
        },
        "event": {
          "anyOf": [
            {
              "$ref": "#/$defs/SwingEvent"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The swing event this anchor is, when it is one. None for a manual pick."
        },
        "source": {
          "$ref": "#/$defs/AnchorSource"
        },
        "reference_frame": {
          "type": "integer"
        },
        "target_frame": {
          "type": "integer"
        },
        "reference_s": {
          "type": "number"
        },
        "target_s": {
          "type": "number"
        },
        "confidence": {
          "description": "How well this instant was located, in the weaker of the two clips. A detected anchor inherits Phase 4's event confidence; a manual anchor is the assertion being fitted rather than an estimate of it, and scores 1.0 -- what checks it is the residual, not this.",
          "maximum": 1.0,
          "minimum": 0.0,
          "type": "number"
        }
      },
      "required": [
        "label",
        "source",
        "reference_frame",
        "target_frame",
        "reference_s",
        "target_s",
        "confidence"
      ],
      "title": "SyncAnchor",
      "type": "object"
    },
    "SyncConfidence": {
      "description": "Why an alignment is or is not trustworthy, decomposed.\n\nThree measured factors and their product, following Phase 4 and Phase 5. The\nproduct is the headline because an alignment needs all three; the factors are\nwhat make a low score actionable, and they fail for different reasons that\ncall for different fixes -- re-film, re-pick the anchors, or supply the right\nslow-motion factor.",
      "properties": {
        "overall": {
          "description": "Product of the three factors below.",
          "maximum": 1.0,
          "minimum": 0.0,
          "type": "number"
        },
        "agreement": {
          "description": "Evidence about whichever estimator actually produced the offset, which is a different quantity for the two kinds of map.\n\nWhere the anchors produced it (`events`, `manual`): how closely they agree with the map, measured against the quantisation floor rather than against zero, taken at its worst alongside the cross-correlation's independent estimate.\n\nWhere the correlation produced it (`combined`, `correlation`): how far the peak sat above its nearest rival. Under realistic landmark noise the anchor residual cannot do this job -- four events scatter tens of milliseconds on a pair that is correctly aligned to under one, and scatter no more than that on two clips of genuinely different swings. The correlation margin separates those cases and the residual does not; `scripts/benchmark_sync.py` has the numbers.\n\n**Zero when nothing checked the map at all** -- an exactly determined fit with no correlation estimate is an assertion, not a measurement, and this is where that shows.",
          "maximum": 1.0,
          "minimum": 0.0,
          "type": "number"
        },
        "anchors": {
          "description": "Mean confidence of the instants the map was fitted to.",
          "maximum": 1.0,
          "minimum": 0.0,
          "type": "number"
        },
        "stability": {
          "description": "How much of the map's accuracy survives to the far edges of the overlap, where its evidence is not. The uncertainty at the pivot over the worst uncertainty anywhere in the overlap, so it measures *only* the cost of extrapolation and does not restate what `agreement` already said about how well the anchors fit.\n\nAn offset-only map scores 1.0 by construction: a constant offset is exactly as good a thousand frames away as at its anchors. A fitted rate pays here for the extrapolation it enables, and that payment is the whole reason `min_rate_span_s` and `rate_significance` exist.",
          "maximum": 1.0,
          "minimum": 0.0,
          "type": "number"
        }
      },
      "required": [
        "overall",
        "agreement",
        "anchors",
        "stability"
      ],
      "title": "SyncConfidence",
      "type": "object"
    },
    "SyncConfig": {
      "additionalProperties": false,
      "description": "Policy for synchronisation.\n\nEvery bound here is **stated policy except where it says otherwise**, in the\nsame sense as `GapPolicy`: none of it is tuned against a ground-truth pair,\nbecause no simultaneous two-camera recording exists in this project yet.\n`scripts/benchmark_sync.py` measures what the method achieves against known\noffsets, which is a different and weaker claim, and says so.",
      "properties": {
        "method": {
          "anyOf": [
            {
              "$ref": "#/$defs/SyncMethod"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Force a method. None picks events when both clips yielded a swing and correlation otherwise, and cross-checks whichever it picked against the other."
        },
        "min_rate_span_s": {
          "default": 1.0,
          "description": "Anchors must span at least this long before a rate is fitted. Below it, the rate's own error does more damage than assuming the clocks agree: a frame of anchor error over half a second is a 6% rate at 30 fps, which is 60 ms of drift a second later.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        "prefer_correlated_offset": {
          "default": true,
          "description": "Take the offset from the cross-correlation when no clock rate was needed, keeping the events for the anchors and the residual.\n\n**Measured, and it overturned the obvious default.** Four swing events look like four clocks and are not: `scripts/benchmark_sync.py` perturbs the landmarks at the noise level Phase 3 measured from real footage and watches where each event lands. Over eight seeds the top moves 8 ms, impact 117 ms, the finish 350 ms and the takeaway 542 ms -- the last two are threshold crossings on a signal that is barely moving there, and one seed puts the takeaway on frame zero. A correlation over the same pair averages several hundred samples and recovers the offset to under a millisecond, against 8-31 ms for the anchored fit.\n\nSo the offset comes from the estimator with the evidence behind it. This is not averaging two estimates -- which this layer refuses to do -- it is using each for the quantity it can determine: a correlation cannot produce a rate or a residual, and four instants cannot out-measure three hundred samples of the same offset.\n\nIt does not apply when a rate *was* fitted. A correlation yields one lag for the whole clip and a rate-bearing map has a different offset at every instant, so substituting one into the other would introduce exactly the bias `SyncQuality.method_disagreement_ms` documents. There the events fit stands whole.",
          "type": "boolean"
        },
        "rate_significance": {
          "default": 2.0,
          "description": "How many of its own standard errors a fitted rate must sit away from 1.0 to be kept. Below it the anchors are consistent with two clocks running in step, and fitting the difference anyway makes the map *worse*: it spends a degree of freedom on noise and buys an uncertainty that grows with distance from the anchors, where a constant offset has none. Two sigma, so a rate is kept when the evidence for it would not be dismissed as chance.",
          "minimum": 0.0,
          "type": "number"
        },
        "max_rate_deviation": {
          "default": 0.5,
          "description": "A fitted rate further than this from 1.0 is refused and the map falls back to offset-only. Half again is already absurd for two clocks; the bound is there to stop a bad anchor pair producing a map that is wrong everywhere rather than wrong in one place.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        "clock_drift_tolerance": {
          "default": 0.02,
          "description": "A fitted rate further than this from 1.0 earns a warning. Consumer camera clocks agree to far better than 2%, so a rate outside it is evidence about the supplied slow-motion factors, not about the hardware.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        "min_overlap_s": {
          "default": 0.3,
          "description": "A candidate lag is not scored unless both clips supply this much signal there. Without it the extreme lags, where a handful of samples overlap, produce spuriously perfect correlations.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        "max_lag_s": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Largest offset the correlation will consider. None searches every lag that satisfies `min_overlap_s`, which is the honest default: two cameras started by hand can be any distance apart."
        },
        "grid_interval_s": {
          "anyOf": [
            {
              "exclusiveMinimum": 0.0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Interval both hand-speed signals are resampled onto before correlating. None uses the finer of the two clips', which is what lets the lag search resolve below the coarser clip's frame interval -- the only thing the faster camera of a mismatched pair contributes to the alignment."
        },
        "rival_separation_s": {
          "default": 0.2,
          "description": "How far from the peak a lag must be to count as a different alignment rather than part of the same peak.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        "max_residual_ms": {
          "default": 150.0,
          "description": "Above this residual the alignment is refused rather than reported with a low score. A tenth of a second is most of a downswing, so a map that wrong is not a worse answer but a different swing. Policy, not a measured optimum.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        "max_anchor_disagreement_s": {
          "default": 0.3,
          "description": "An event pair whose offset differs from the median pair's by more than this is dropped before fitting, and said so. One misdetected event should cost its own anchor, not the whole alignment.\n\n**Measured, not assumed.** The bound has to admit the spread a genuine clock rate produces while catching an event located in the wrong place, and those two overlap on paper: a rate of 0.7 over a 1.6 s swing moves the anchors 0.4 s apart. In practice they do not, because the median sits among the clustered anchors rather than at one end. `scripts/benchmark_sync.py` sweeps the supplied slow-motion factor from 2% to 40% wrong and keeps all four anchors at this bound, recovering the rate to within 0.3%; the same bound drops the spurious takeaway a clip that starts mid-backswing produces, which sits 0.47 s out.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        }
      },
      "title": "SyncConfig",
      "type": "object"
    },
    "SyncMethod": {
      "description": "What produced a time map.\n\nMANUAL\n    Anchors supplied by a person, who looked at both clips and said which\n    frames show the same instant. The engine does the arithmetic and the\n    error accounting; it does not second-guess the instants.\n\nEVENTS\n    Anchors from Phase 4's detected swing events, paired by name. The only\n    method that can estimate a rate, because it is the only one that\n    produces anchors spread across the swing.\n\nCORRELATION\n    The lag that maximises the normalised correlation of the two hand-speed\n    signals. Produces one number -- an offset -- and no rate.\n\nCOMBINED\n    The default where both are available, and each is used for what it can\n    actually determine: the **offset from the correlation**, the anchors and\n    the residual from the events. See `SyncConfig.prefer_correlated_offset`\n    for the measurement that decided this.",
      "enum": [
        "manual",
        "events",
        "correlation",
        "combined"
      ],
      "title": "SyncMethod",
      "type": "string"
    },
    "SyncModel": {
      "description": "Everything the engine concluded about aligning two clips.\n\n`aligned` is false whenever no map could be produced or the map produced was\ntoo poor to report, and then `time_map` is None rather than populated with a\nguess. A caller that reads the map without checking cannot therefore mistake\na refusal for an alignment at zero offset, which is the failure mode this\nshape exists to prevent -- and zero offset is exactly the wrong value to\ndefault to, because it is also a perfectly plausible answer.",
      "properties": {
        "schema_version": {
          "default": 1,
          "type": "integer"
        },
        "aligned": {
          "type": "boolean"
        },
        "method": {
          "anyOf": [
            {
              "$ref": "#/$defs/SyncMethod"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "What produced the map. None when nothing did."
        },
        "reference": {
          "$ref": "#/$defs/ClipRef"
        },
        "target": {
          "$ref": "#/$defs/ClipRef"
        },
        "time_map": {
          "anyOf": [
            {
              "$ref": "#/$defs/TimeMap"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "anchors": {
          "items": {
            "$ref": "#/$defs/SyncAnchor"
          },
          "type": "array"
        },
        "residuals": {
          "items": {
            "$ref": "#/$defs/AnchorResidual"
          },
          "type": "array"
        },
        "quality": {
          "anyOf": [
            {
              "$ref": "#/$defs/SyncQuality"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "confidence": {
          "anyOf": [
            {
              "$ref": "#/$defs/SyncConfidence"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "correlation": {
          "anyOf": [
            {
              "$ref": "#/$defs/CorrelationReport"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The independent estimate, when one could be computed. Never merged into the map."
        },
        "overlap": {
          "anyOf": [
            {
              "$ref": "#/$defs/OverlapReport"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "config": {
          "$ref": "#/$defs/SyncConfig"
        },
        "refusal": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Why no map was produced, in words. Set exactly when `aligned` is false."
        },
        "warnings": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "aligned",
        "reference",
        "target",
        "config"
      ],
      "title": "SyncModel",
      "type": "object"
    },
    "SyncQuality": {
      "description": "How well the alignment is determined, in milliseconds.\n\n`quantisation_floor_ms` is the bound nothing about this pair can beat. An\ninstant located to the nearest frame carries a uniform error of a frame\ninterval, whose standard deviation is that interval over sqrt(12); two clips\ncontribute one each, in quadrature. A residual at the floor means the anchors\nagree as well as their frame rates allow, and the coarser clip is what sets\nit -- pairing a 240 fps camera with a 30 fps one buys almost nothing.",
      "properties": {
        "residual_rms_ms": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "RMS of the anchor residuals. **None when the fit has no spare degrees of freedom**, where the residual is zero by construction and reporting it would look like a perfect alignment."
        },
        "residual_max_ms": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Largest absolute anchor residual, on the same condition."
        },
        "degrees_of_freedom": {
          "description": "Anchors minus fitted parameters. Zero or less means the residual is not evidence.",
          "type": "integer"
        },
        "quantisation_floor_ms": {
          "description": "Best residual the two clips' frame rates permit. See the class docstring.",
          "type": "number"
        },
        "method_disagreement_ms": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Distance between the fitted offset and the cross-correlation's, at the pivot. None when only one estimate exists, which is itself worth knowing: nothing independent checked the map.\n\n**Comparable only when the map is offset-only.** A correlation produces one lag for the whole clip; a map with a fitted rate has a different offset at every instant, and the two are then estimating different quantities. A rate of 0.91 moves the map by 90 ms per second away from the pivot, so a correlation landing 9 ms from the pivot value is close agreement rather than poor. It is reported in both cases and drives `SyncConfidence.agreement` in neither, once a rate is in play -- there the residual is the better instrument, being the only one that is rate-aware."
        }
      },
      "required": [
        "degrees_of_freedom",
        "quantisation_floor_ms"
      ],
      "title": "SyncQuality",
      "type": "object"
    },
    "TimeMap": {
      "description": "The affine map from reference-clip time to target-clip time.\n\nBoth directions are provided because both are needed: rendering the target\nclip alongside the reference needs `to_target`, and reporting an instant\nfound in the target in the reference's frame numbering needs `to_reference`.",
      "properties": {
        "offset_s": {
          "description": "How far ahead of the reference clock the target clock runs, at `pivot_s`. With `rate` at 1.0 this is the whole map.",
          "type": "number"
        },
        "rate": {
          "description": "Target seconds per reference second. 1.0 means the two clocks run at the same speed, which is the default and is not a measurement unless `rate_estimated` is true.",
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        "rate_estimated": {
          "description": "True when the rate was fitted from anchors spanning enough time to determine it. False means it was held at 1.0, which is an assumption about the hardware rather than something measured here.",
          "type": "boolean"
        },
        "pivot_s": {
          "description": "The reference-clip instant `offset_s` is stated at: the centroid of the anchors. Chosen so the offset and rate uncertainties are uncorrelated and can be combined by `uncertainty_at`.",
          "type": "number"
        },
        "offset_uncertainty_s": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Standard error of `offset_s`, from the fit's residual scatter where there are spare degrees of freedom and from the frame quantisation otherwise. None where neither is available."
        },
        "rate_uncertainty": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Standard error of `rate`. None whenever the rate was not estimated."
        },
        "support_start_s": {
          "description": "Earliest reference time an anchor was placed at.",
          "type": "number"
        },
        "support_end_s": {
          "description": "Latest reference time an anchor was placed at.",
          "type": "number"
        }
      },
      "required": [
        "offset_s",
        "rate",
        "rate_estimated",
        "pivot_s",
        "support_start_s",
        "support_end_s"
      ],
      "title": "TimeMap",
      "type": "object"
    }
  },
  "description": "A session: the clips of one swing, and how their clocks relate.\n\n`clips` is a list rather than a fixed pair. Two is what Phases 7 to 9 need\nand what the capture protocol asks for, but nothing here is made harder by\nallowing three, and a fixed pair would have to be widened later by changing\nthe stored shape rather than by adding a row.",
  "properties": {
    "schema_version": {
      "default": 2,
      "type": "integer"
    },
    "id": {
      "type": "integer"
    },
    "name": {
      "type": "string"
    },
    "notes": {
      "default": "",
      "type": "string"
    },
    "created_at": {
      "format": "date-time",
      "type": "string"
    },
    "clips": {
      "items": {
        "$ref": "#/$defs/ProjectClip"
      },
      "type": "array"
    },
    "syncs": {
      "items": {
        "$ref": "#/$defs/ProjectSync"
      },
      "type": "array"
    },
    "rig": {
      "anyOf": [
        {
          "$ref": "#/$defs/CameraRig"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "What is known about this project's cameras: their intrinsics, and their relative pose where it has been measured. None until a calibration is stored, which is the ordinary state -- an uncalibrated project is fully supported and simply makes no metric-scale claims.\n\nOne rig per project rather than one per clip, because extrinsics relate two cameras and belong to neither. It is read whole and never queried by part, which is why it is stored as a document."
    },
    "warnings": {
      "description": "Facts about reading this project back. A stored sync written under a schema version this build does not understand is reported here and left out of `syncs`, rather than making the whole project unopenable over one stale row.",
      "items": {
        "type": "string"
      },
      "type": "array"
    }
  },
  "required": [
    "id",
    "name",
    "created_at"
  ],
  "title": "Project",
  "type": "object"
}
