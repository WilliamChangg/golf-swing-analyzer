{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set: the frontend may call the app's own commands, and may open a native file picker to choose a video to import. No filesystem, shell, or network permissions are granted — the picker returns a path, which the frontend hands to Rust and Rust hands to the analysis engine, so the WebView itself never reads a file. Permissions are added as features need them, not in advance.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
